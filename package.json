{
  "name": "video-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "test": "node --test test/*.test.js",
    "deploy": "wrangler deploy"
  }
}
